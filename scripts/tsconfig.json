{
  "compilerOptions": {
    // Target latest ECMAScript.
    "target": "ESNext",
    "lib": ["ESNext"],
    "module": "ESNext",

    // Configuration required for Node.
    "moduleResolution": "node",
    "esModuleInterop": true,

    // Miscellaneous configuration.
    "strict": true, // Enable strict type checking.
    "ignoreDeprecations": "6.0", // Keep current Node-style resolution until migrating imports.
    "noEmit": true, // Don't emit JS files - build step only for type checking.
    "resolveJsonModule": true, // Allow importing JSON files.
    "skipLibCheck": true, // Skip type checking for external libraries.

    // TODO: Replace with yarn workspaces.
    "rootDirs": [".", "../website/src"]
  },
  "include": ["."]
}
